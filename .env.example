# Example environment variables for Docker Compose / Dokploy
# Copy to .env and customize for your environment.

# Postgres
POSTGRES_USER=pos
POSTGRES_PASSWORD=un_password_fuerte
POSTGRES_DB=posdb

# API
JWT_SECRET=un_secret_fuerte
# Optional: override API port
# PORT=4000

# Web
# If you expose the API publicly, set this to your public API URL, e.g. https://api.tu-dominio.com
# If running in the same docker network, you can keep http://api:4000
NEXT_PUBLIC_API_URL=http://api:4000
